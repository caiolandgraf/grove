package seeders

import (
	"fmt"

	"gorm.io/gorm"
)

// {{.Name}}Seeder seeds initial {{.Name}} data.
type {{.Name}}Seeder struct{}

func ({{.Name}}Seeder) Name() string { return "{{.Name}}Seeder" }

func ({{.Name}}Seeder) Seed(db *gorm.DB) error {
	// TODO: implement seeding logic using your module repo, e.g.:
	//   repo := users.Users(db)
	//
	// Keep the seeder idempotent: running it multiple times should be safe.
	_ = db

	return fmt.Errorf("{{.Name}}Seeder: not implemented")
}
