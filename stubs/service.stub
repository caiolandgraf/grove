package {{.Package}}

import "gorm.io/gorm"

type Service interface {
	// TODO: add business methods (Create{{.Name}}, Get{{.Name}}ByID, etc.)
}

// Store abstracts persistence for {{.NameLower}} operations (enables mocks in tests).
type Store interface {
	// TODO: add store methods matching your Repo
}

type service struct {
	store Store
}

func NewService(store Store) Service {
	return &service{store: store}
}

func WireService(db *gorm.DB) Service {
	return NewService({{.PluralName}}(db))
}
